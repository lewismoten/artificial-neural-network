(() => {

  'use strict';
// momentum α
  module.exports = (network) => {

    // gradient checks
    /*
    (d*f(x) / (d*x))
    (f(x + h) - f(x - h)) / 2h
    */
    // gradient check - turn off dropout / augmentations

    // sanity checks
    //
    //
    // walker(layer.weights, (weight, x, y) => {
    //
    //   layer.previous.weights[x][y] = weight;
    //
    // });
    // layer.previous.biases = layer.biases.slice(0);


  }

})();
